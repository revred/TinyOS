# REPORT-2026-07-30-03 — The Ti64 Console V1 Is Live Against the Host: V1.0 Static Parity + V1.1 Wired Seams, Keyboard-Driven Smoke Green

**Test(s) covered:** `TEST-P0-01-02-A` (`STORY-P0-01-02` — whole-system context and
assurance-spine integrity), the anchor context/decision reports use: this is the dated record
of [`06A`](../../session/hand-2026-07-30/06A-ux-v1-delivery-mandate.md)'s phases V1.0 and
V1.1 — the binding [`work/UX-V1/V1-STRATEGY.md`](../../work/UX-V1/V1-STRATEGY.md) delivery —
on the `goals/reports/` pattern. **No new Feature or Story rows: the console is `EPIC-H4`-lane
host tooling (the 17G/REPORT-2026-07-30-01 precedent); the two `stage-e-console` additions
(`reserved_text` V1 format, `open_at` slot naming) land as ordinary red-first library tests
(`reserved_text_is_the_v1_system_line`, `t8_open_at_names_the_enumerated_slot`).** The
`STORY-UX-*` documents enter the spine with V1.2's red-first tranche, per
[Handover 07A](../../session/hand-2026-07-30/07A-ux-v1-0-1-executed.md) §5.
**Date:** 2026-07-30 · **Tier:** host-side for the console (the parity leg boots the real
kernel under QEMU; every windowing/UX property proven is a host-side composition property).

## What this report claims

1. **V1.0 — static reference parity** (commit `de0a9d4`): `console.css`,
   `console-core.js`, `console-v2.js`, `console-bodies.js` and `index.html` are
   byte-identical copies of the `work/UX-V1/` reference (`cmp` after each copy), so pixel
   parity against `v3-console.html` holds by construction. `reserved.html` restyled to the
   `.reserved` tokens (28 px, no badge), contract untouched.
2. **V1.1 — the Part F seams, wired** (this report's commit): the browser-side `Session`
   is deleted; `open_tab`/`run_line`/`read_tab`/`read_console`/`focus_tab` carry the
   console; the system line is painted only by `main.rs::reconcile`; every transcript
   pixel is the real Rust core's output, classified and coloured renderer-side (LE-59).
3. **The keyboard-driven smoke passes end to end** — `window.smokeKey` (`STORY-UX-04`'s
   hook) drives the grammar; no mouse. Aggregate `"smoke": "pass"`, exit 0.

## Evidence table (assets under [`assets/2026-07-30-ux-v1/`](assets/2026-07-30-ux-v1/))

| # | Claim | Verdict | Evidence |
| - | ----- | ------- | -------- |
| 1 | Boot (cockpit, 1440×860): tx01 pre-runs `VER · DIR · SET · TASKMGR · TASKKILL RT-CTRL · TYPE README.TXT`; coloured transcript; the TASKKILL denial deep red in transcript AND the evidence column's authority ledger; system line `tx01 MS-DOS — 1 tab(s) — parity: not run — authority denials: 1` host-painted | **PASS** | `01-boot-cockpit.png`; `smoke.json` check `boot_prerun_and_taskkill_denial_in_tx01: true` |
| 2 | `Ctrl+T` opens a second MS-DOS session; strip name, host identity and system line are ONE name (`tx04` ↔ `tab-4`/`TAB-4`) | **PASS** | `02-second-dos-via-ctrl-t.png`; `smoke.json` `tabs[]`; lib test T8 |
| 3 | Isolation typed at the prompt: `SET GREET=…` in tx04, `Environment variable GREET not defined` in tx01 — per-tab `World`, visibly | **PASS** | `03-set-in-new-tab.png`, `04-isolation-in-tx01.png`; checks `set_ran_in_new_tab`, `isolation_visible_in_tx01` |
| 4 | `SAMPLE.TCB` typed by name runs the real `.TCB` runner in tx01 | **PASS** | `05-sample-tcb.png`; check `sample_tcb_ran_in_tx01: true` |
| 5 | `F8` runs the real three-signal parity suite from the parity tab's own identity; all three signals green; OVERALL PASS; system line follows (`tx05 PARITY — 3 tab(s) — parity: PASS`) | **PASS** | `06-parity-wall.png`; `smoke.json` `parity.fixture_signal/transcript_signal/spoor_signal: true`, `overall: Pass` |
| 6 | Resolver denials visible, never swallowed: `format_disk` (unlisted) and `run_line` (tab verb) from the chrome identity are refused at the authority seam and painted deep red into a transcript-rendering tab | **PASS** | `07-denials-visible.png`; check `denials_visible: true`; `smoke.json` `denials[]` with runtime-derived identity tuples |
| 7 | Master menu (`Ctrl+Esc`) and keyboard map (`F1`) open from the keyboard | **PASS** | `08-master-menu.png`, `09-keyboard-map.png` |
| 8 | Grant tables stay disjoint through the real resolver; reserved label holds no verbs | **PASS** | `cargo test -p stage-e-console` — 27 lib + 7 e2e green (R1–R6, E1–E8 incl. E7/E8 unchanged) |

## Bounds and non-claims

Nothing here is `PD-*`/`TG-P*`/timing evidence. The CPU meter is a shape wired to
`--t-pending` (`ADR 0005`; no platform qualified). The GUI flavour is not a compositor
(`LE-53` stands). rt/agent tabs are display models with their caveat lines on screen
(Phases 3/6). The full Part H checklist run (all 15 items) is the close-out of V1.3, not
this report; the divergence notes an acceptance runner needs first are
[Handover 07A](../../session/hand-2026-07-30/07A-ux-v1-0-1-executed.md) §3 — including
that Part H item 15's ≤ 40 KB payload figure cannot hold against the verbatim reference.

## Register interlock

`LE-59` **closed** by this delivery (renderer-side classification + Campbell semantic
tokens; no payload escape is ever interpreted). `LE-60`/`LE-61` remain open with partial
surface delivered; `LE-57` untouched (no `HELP` anywhere in the UI); `LE-58` untouched.
