# REPORT-2026-07-26-29 — STORY-P0-04-03 Read-Only PCI Bus-0 Enumeration (Host + Tier 0)

**Test(s) covered:** [`TEST-P0-04-03-A`](../tests/TEST-P0-04-03-A.md) (`STORY-P0-04-03`, `FEAT-P0-04`).

**Result:** **Pass.**

- `cargo test -p hal --lib`: **8/8** (4 new `device::tests` — fixed-capacity `DeviceTable` fail-closed semantics and the identity-and-position-only descriptor guard — alongside the 4 pre-existing `topology::tests`).
- `cargo test -p hal-x86_64 --lib`: **47/47** (10 new `pci::tests` — BDF validation, CAM address packing against a worked q35 SMBus example, header-field decoding, empty-bus bounded walk, identity/position recording, multifunction probing, single-function non-probing, offset-restriction observation, capacity-overflow fail-closed, and cross-walk determinism).
- `cargo test --workspace --lib`: **172/172** (`exec` 51, `hal` 8, `hal-x86_64` 47, `kernel` 66) — the concurrent whole-system-context session's own additions included; nothing regressed.
- `cargo test -p xtask`: **23/23** after updating `committed_assurance_spine_is_complete`'s hardcoded expectations to **24 Tests / 29 Reports** for this Story's `TEST-P0-04-03-A` and this report.
- `cargo run -p xtask -- qemu-x86_64 --fixture=pci-enumeration`: **exit 0 (Success)** — under QEMU `q35`, the legacy-CAM walk found the host bridge at 0:0:0 with vendor `0x8086`, discovered a non-empty bus-0 device set, and a second complete walk produced a byte-identical table (read stability).
- `cargo run -p xtask -- qemu-x86_64` (default real boot, which now gates on bus-0 discovery succeeding non-empty after ACPI topology + `interrupts::init`): **exit 0 (Success)**.
- Regression check on every pre-existing QEMU fixture (`--fixture=broken-boot` (still correctly exits Failure), `--fixture=context-switch`, `--fixture=address-space`, `--fixture=win32-shim`, `--fixture=blue-sharc`, `--fixture=blue-sharc-broken`, `--fixture=shared-memory`, `--fixture=idt-apic-timer`, `--fixture=idt-apic-unrouted` (still correctly exits Failure — its documented pass condition)) — all unchanged. **One exception, not caused by this Story:** `--fixture=pool-bench`, added by the concurrent whole-system-context session while this Story was in flight, exits with harness error 2 ("QEMU exited with unexpected code 0") both before and after this Story's changes touch nothing on its path — recorded here for the next session rather than silently absorbed or silently skipped.
- `cargo fmt --check` (workspace): clean.
- `cargo clippy --workspace --lib -- -D warnings`: clean.
- `cargo run -p xtask -- check-crate-sizes`: all crates under the 20,000-line ceiling (`hal` 183, `hal-x86_64` 1,773, `kernel` 2,567 — the largest).
- `cargo run -p xtask -- check-image-size`: kernel image **18,024 bytes** against the 8 MiB ceiling (17,288 before this Story; +736 bytes for the enumeration walk and its boot-path gate).
- `cargo run -p xtask -- check-assurance-spine`: passes — 8 Features, 25 Stories, **24 Tests**, **29 Reports**, 5 containment classes, 20 boundary tests, 20 security controls, 14 Protection Domain contracts, 14 code-admission gates, 25 class communication pairs, 19 application/platform targets, 9 landing zones; `STORY-P0-04-03`'s contract row now selects `D01,D22` / `SEC-13,SEC-18,SEC-19,SEC-20` / `C0,C1,C2` at `baseline-debt` (was `specified` with an ARM64 copy-paste rationale — corrected).
- `cargo run -p xtask -- check-performance-catalogue`: 625/625.

**Root cause found and fixed during bring-up:** none — the fixture passed on its first QEMU run. The one design correction made before any code ran: the Story's pre-written contract row selected `D08` (paging and address spaces) and `SEC-03` (per-process address-space isolation), both irrelevant to a read-only discovery walk that touches no page tables and creates no address space; the row was corrected to `D01`/`D22` (boot/topology discovery, opt-in driver lifecycle) and `SEC-13,SEC-18,SEC-19,SEC-20` — `SEC-13` because discovery-must-not-select-a-driver is exactly its invariant — matching `STORY-P0-04-01`'s hardware-discovery selections.

**Environment/tier:** Windows 11 host (host unit tests) + QEMU `q35` Tier 0 (fixture and real-boot runs), per the Target Hardware & Test Matrix.

**Toolchain/commit:** pinned `nightly-2026-07-26`; dirty working tree shared live with a concurrent whole-system-context session, so no commit hash is claimed.

**Linked session:** [`session/hand-2026-07-26/35-story-p0-04-03-pci-bus-enumeration.md`](../../session/hand-2026-07-26/35-story-p0-04-03-pci-bus-enumeration.md).

**Deliberately not done:**

- No ECAM/MCFG (MMIO) configuration access — legacy CAM port I/O only, chosen because it needs no MCFG parsing and no boot-time page-table extension; q35's ECAM window at `0xB0000000` sits outside `boot.rs`'s current identity maps. Named in the Story.
- No traversal behind PCI-to-PCI bridges — bus 0 only; q35's default machine exposes every device there. Named in the Story.
- No BAR, class-code, capability-list, or interrupt-line reads; no driver selection; no device state mutation — discovery-only, per acceptance criterion 2 and `FEAT-P0-04`'s containment contract.
- No raw performance/security evidence (latency tails, WCET, hostile-device campaigns, IOMMU confinement) — this Story's assurance contract remains `baseline-debt`, not `verified`, per the standing assurance-spine rule.
