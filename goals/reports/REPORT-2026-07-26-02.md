# REPORT-2026-07-26-02

**Test(s) covered:** [`TEST-P0-01-03-A`](../tests/TEST-P0-01-03-A.md) (`xtask qemu-x86_64` command smoke test)

**Result:** **Pass.** `cargo run -p xtask -- qemu-x86_64` builds and boots the kernel successfully, exiting `0` (`KernelBootSucceeded`). `cargo run -p xtask -- qemu-x86_64 --fixture=broken-boot` builds the kernel with the `fixture-broken-boot` Cargo feature enabled (a deliberate `panic!()` immediately on entering `kernel_main`), boots it, and observes QEMU exit code 35 (`(0x11 << 1) | 1`, the failure isa-debug-exit code), which `xtask` maps to its own distinguishable exit code `1` (`KernelBootFailed`) — proving the pass/fail distinction the Story's acceptance criteria require, not just that the command runs. A third class of failure (harness/QEMU misconfiguration, e.g. QEMU not found or an unrecognized exit code) maps to exit code `2` (`HarnessError`), verified by code inspection (`os/src/xtask/src/main.rs`); a real QEMU-absent scenario was exercised naturally earlier in this session before QEMU was installed.

**Environment/tier:** CI / Tier 0, per the Story's classification. Exercised locally on Windows (this session's dev machine); not yet exercised in the GitHub Actions CI pipeline (`.github/workflows/ci.yml`'s `kernel-boot-x86_64` job runs on `ubuntu-latest` and has not yet had a CI run triggered as of this Report).

**Toolchain/commit:** `nightly-2026-07-26`; workspace commit base `167db4f4dc642c82d42336b6b4e621e53e57964c`.

**Linked session:** [`session/hand-2026-07-26/`](../../session/hand-2026-07-26/).

**Note:** the CI job for this Test has been authored but not yet observed to pass in an actual CI run — this Report reflects local execution only. A follow-up Report should be filed once the first CI run against this workflow completes, per `goals/reports/README.md`'s "first time a new Test passes in CI" trigger.
