# REPORT-2026-08-01-02 — `STORY-P0-01-10`: a Specified header cannot outlive its own passing Report, and half of `LE-65`'s proposal did not survive contact with the register

**Test(s) covered:** `TEST-P0-01-10-A` (`STORY-P0-01-10` — a Specified header cannot outlive its own passing Report), performance domain `D01`, mapped `SEC-19`/`SEC-20`, containment classes `C0`/`C1`, boundary tests `BND-01`, `BND-02`, `BND-03`, `BND-17`, `BND-18`.
**Date:** 2026-08-01 · **Tier:** Host only — the subject is the agreement between documents in this repository.
**Toolchain/commit:** `nightly-2026-07-26`, against `e71b30b`.
**Linked session:** [`session/hand-2026-08-01/03A-the-dashboard-moves-because-the-register-moved.md`](../../session/hand-2026-08-01/03A-the-dashboard-moves-because-the-register-moved.md), executing [`02C`](../../session/hand-2026-08-01/02C-next-session-mandate-the-dashboard-moves-itself.md) §2 Step 3. **`LE-65` is closed.**

## Result

**Pass**, all four clauses. Assurance state stays `baseline-debt`; no performance guardrail closed and no Story became assurance `verified`.

`check-assurance-spine` now reads every Report's `## Result` opener and `Test(s) covered:` join and refuses the tree when a `Specified` Story header is covered by a Report recording a pass. The summary line prints how many passing Reports were cross-checked. Host tests **262**, from 255 — the seven `LE-65` cases, each refusal beside its acceptance.

`cargo fmt --all -- --check` clean; workspace clippy (`x86_64-unknown-linux-gnu`) and the AArch64 job clean.

## The finding: the refusable class is narrower than the row proposed

`LE-65`'s owner-path proposed refusing `Specified` **and** `In progress` headers with passing Reports. The `In progress` half is wrong, and the register itself proves it: [`REPORT-2026-07-30-01`](REPORT-2026-07-30-01.md) records **PASS on all four** of its mandate's criteria while its `FEAT-P2` Stories deliberately stay `In progress`, because their `D23`/`D14` performance numbers are stated open debt (`LE-35`'s machinery). A gate refusing that pairing would refuse honest stated-debt delivery — the exact opposite of what this register is for. The row was amended with the finding and closed on the `Specified`-only gate: a filed passing Report contradicts *"not started"* outright, whatever fraction of the Story it covers; it contradicts *"in progress"* not at all.

## Raw evidence

### Clause 1 — the refusal

The `-08` incident's exact shape, fabricated and refused: a Report whose `Test(s) covered:` names `TEST-P0-01-08-A`, whose Result opens `**Pass**`, above a header reading `Specified` — refused naming the Story, the Report, and the fix direction (*re-verify, then advance — verify, don't inherit*, Handover 35's rule). Coverage through the Test id alone refuses identically; the trailing test letter resolves to the Story id.

### Clause 2 — the deliberate non-refusals

`In progress` beside the same passing Report: accepted, with the `REPORT-2026-07-30-01` precedent restated in the gate's own documentation so a later reader does not "complete" the check into refusing honest partial delivery. A resultless Report (the 2026-07-26 generation's shape): no verdict extracted, nothing refused, and not counted as cross-checked.

### Clause 3 — only an unambiguous pass triggers

The verdict grammar test: `**Pass**` and `**PASS on all four**` read as pass; `**Blocked**` reads as blocked and never matches; an unbolded `Pass` and a Result-less document read as no verdict at all.

### Clause 4 — every refusal demonstrated

Seven host tests, each refusal with its acceptance case beside it. The committed tree passes by construction — Step 1 of this session already corrected the one live instance — so the green run is claimed as evidence of nothing; the tests are the evidence. Against the current tree the gate cross-checks **8 passing Reports** (the modern bolded-Result generation, this one and `-01` included) and finds no contradiction, a number the summary line now prints rather than hides.

## Assurance IDs

| ID | Status |
|---|---|
| `SEC-19` (auditable decisions) | **Pass, structural.** A state claim can no longer contradict the Story's own filed evidence in the one direction no gate looked; the refusal names the fix |
| `SEC-20` (bounded resources) | **Pass, structural.** One bounded pass over files report-coverage already reads |
| `BND-01`, `BND-02`, `BND-03`, `BND-17`, `BND-18` | **Deferred**, as for every Phase-0 governance Story. This Story parses repository text, not hostile input |
| `PERF-D01-G01..G25` | **Unevidenced.** No gate closed |

## Security under load

Not applicable and not claimed. Nothing here runs at a measured performance point, admits code, crosses a containment boundary, or grants authority.

## Named debt this Report leaves open

- **The verdict grammar is one bolded opener.** A pass recorded in any other shape is invisible to this gate; a stricter contract belongs in the Report schema if ever wanted.
- **`In progress` headers can sit above fully-passing Reports indefinitely.** Deliberate, per the finding above — distinguishing "all clauses" from "the delivered half" is a prose judgment this gate refuses to guess at.
- **`LE-34` remains open** — the last named member of the prose-versus-register family in this Feature's reach.
- **No performance guardrail closes and no Story's assurance state moves.**
