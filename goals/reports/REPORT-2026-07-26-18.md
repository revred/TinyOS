# REPORT-2026-07-26-18

**Test(s) covered:** `TEST-P0-06-04-A` (`STORY-P0-06-04` — wire `kernel::wcet` to emit spoors on overrun/reset, adopting spoor into a second Phase 0 subsystem beyond `FEAT-P0-06`'s own already-met exit criteria).

**Result:** **Pass.**

- `cargo test -p kernel --lib` — 51/51 passing (up from 49 in `REPORT-2026-07-26-17`: two new test functions, `resetting_an_already_zero_counter_stamps_nothing` and `reset_budget_window_against_an_unknown_task_fails_closed`, covering `reset_budget_window`'s two no-stamp/fail-closed paths that didn't previously have a dedicated assertion). All existing `wcet::tests` pass with the new `&mut SpoorJournal<J>` parameter threaded through `record_tick`/`reset_budget_window`.
- `cargo test --workspace --lib` — 112/112 passing (up from 110): `hal` 4, `hal-x86_64` 26, `kernel` 51, `exec` 31.
- Regression check on all five pre-existing QEMU fixtures: `cargo run -p xtask -- qemu-x86_64` (no `--fixture`), `--fixture=broken-boot` (exits `KernelBootFailed` by design), `--fixture=context-switch`, `--fixture=address-space`, `--fixture=win32-shim` — all still pass unchanged. This Story added no QEMU fixture and no production call site (no dispatcher/timer loop is wired into `main.rs` yet, same gap `REPORT-2026-07-26-17` and `kernel::wcet`'s own module doc comment already named) — the release kernel ELF is unchanged at 16,032 bytes, confirmed by a clean rebuild (`cargo build --release -p kernel --target targets/x86_64-tinyos.json -Z json-target-spec -Z build-std=core,compiler_builtins -Z build-std-features=compiler-builtins-mem`).
- `cargo fmt --check` (workspace) — clean.
- `cargo clippy --workspace --lib -- -D warnings`, restricted to host-buildable lib targets (`kernel`'s and `exec`'s `[[bin]]` targets still can't be host-compiled on this Windows dev machine — same pre-existing, already-documented limitation, not a regression) — clean.
- `cargo run -p xtask -- check-crate-sizes --ceiling=20000` — `kernel` 1630 lines (up from 1583 — `wcet.rs`'s new journal parameters, stamped-event doc comments, and two new no-stamp test functions), `exec` 1198, `hal-x86_64` 750, `hal` 91, `xtask` 659 — all comfortably under the ceiling.

**Environment/tier:** Host only (`cargo test -p kernel --lib`), per `TEST-P0-06-04-A`'s own scope — `kernel::wcet`/`kernel::spoor_journal` are pure logic with no target-specific dependency.

**Toolchain/commit:** `nightly-2026-07-26` (workspace-pinned toolchain, per `os/rust-toolchain.toml`); `cargo 1.96.0`, `rustc 1.96.0`.

**Linked session:** [`session/hand-2026-07-26/24-story-p0-06-04-spoor-adoption-kernel-wcet.md`](../../session/hand-2026-07-26/24-story-p0-06-04-spoor-adoption-kernel-wcet.md).

**Deliberately not done:**

- **No production call site.** No dispatcher/timer loop is wired into `main.rs` yet — `kernel::capacities`' own stated convention ("only capacities with a real, concrete production or Tier-0-fixture consumer are declared here today") means no `SpoorJournal<N>` capacity constant or D11-G09/G10 footprint measurement is added yet either; that's the next natural step once a real caller picks a concrete `N`, not before — unchanged from `REPORT-2026-07-26-17`'s own scope note.
- **No storage-backed journal, no query/replay/summarize API** — unchanged from prior reports' own scope notes.
