# REPORT-2026-08-01-01 — `STORY-P0-01-09`: the numerics that survived `-08` become generated or gated, and the tabstrip was already wrong when the gate arrived

**Test(s) covered:** `TEST-P0-01-09-A` (`STORY-P0-01-09` — the numerics that survived `-08` become generated or gated), performance domain `D01`, mapped `SEC-19`/`SEC-20`, containment classes `C0`/`C1`, boundary tests `BND-01`, `BND-02`, `BND-03`, `BND-17`, `BND-18`.
**Date:** 2026-08-01 · **Tier:** Host only — the subject is the agreement between two files in this repository.
**Toolchain/commit:** `nightly-2026-07-26`, against `d2c9969`.
**Linked session:** [`session/hand-2026-08-01/03A-the-dashboard-moves-because-the-register-moved.md`](../../session/hand-2026-08-01/03A-the-dashboard-moves-because-the-register-moved.md), executing [`02C`](../../session/hand-2026-08-01/02C-next-session-mandate-the-dashboard-moves-itself.md) §2 Step 2.

## Result

**Pass**, all five clauses. Assurance state stays `baseline-debt`; no performance guardrail closed and no Story became assurance `verified`.

`cargo run -p xtask -- emit-dashboard` now prints two marked blocks — `overall-progress` and `-08`'s `stat-row` — and `check-assurance-spine` byte-compares both regions and additionally gates the tabstrip's two counts, the progress bar width, the footnote's four state counts, and the Epic-denominator sentence.

`cargo fmt --all -- --check` clean. Host tests **255**, from 241 — fourteen added, every refusal with an acceptance case beside it.

## What the gate found on first contact

**The committed tabstrip was already stale when the gate arrived.** With the checks implemented and the page untouched, `check-assurance-spine` refused the tree with:

```text
goals/index.html's tabstrip does not state the current decomposed-Epic count.
Expected the Epics tab label to open with `Epics <span class="count">4 decomposed` (STORY-P0-01-09)
```

The tabstrip said **3 decomposed** directly beneath a tile saying **4 / 12** — hand-maintained numerics disagreeing with each other on the same screenful, one refresh after the page was last hand-synced (2026-08-01, `fc04161`). The same failure class the Story was decomposed against, observed live before the fix, which is the red `TEST-P0-01-09-A` clause 4 demands. The two committed-tree host tests (`committed_assurance_spine_catalogues_are_exact`, `committed_assurance_spine_population_never_shrinks`) failed on the same refusal at the same moment. Fixed toward the derivation; nothing grandfathered (clause 5).

## The derivations, grounded

| | Derived | Verified |
|---|---|---|
| Roadmap Epic population (`EPIC-P*` docs ∪ backlog phase table; `EPIC-H*` excluded) | **12** | ✔ agrees with the page's hand-written "denominator is now 12" |
| Of those, decomposed (≥ 1 Story contract row) | **4** — `P0`, `P1`, `P2`, `P9` | ✔ agrees with the tile, refutes the tabstrip's 3 |
| Stories `Verified` + `Functionally Verified` | **39 + 11 = 50 of 71** | ✔ from the same headers the badge gate reads |
| Progress bar width | **70%** (integer-rounded 50/71) | ✔ replaces the hand-tuned percentage |

Horizon Epics are excluded from the population on the backlog's own authority — its *Destination horizons* section states those ids "are not inserted into the numbered critical path and do not imply sequence" — so `EPIC-H2.md` existing on disk does not move the denominator the tile argues about.

## Assurance IDs

| ID | Status |
|---|---|
| `SEC-19` (auditable decisions) | **Pass, structural.** Five more of the page's figures can no longer contradict the register, and every refusal prints the expected text verbatim |
| `SEC-20` (bounded resources) | **Pass, structural.** One bounded pass over files the checker already reads, plus one read of `backlog.md` |
| `BND-01`, `BND-02`, `BND-03`, `BND-17`, `BND-18` | **Deferred**, as for every Phase-0 governance Story. This Story parses repository text, not hostile input |
| `PERF-D01-G01..G25` | **Unevidenced.** No gate closed |

## Raw evidence

### Clause 1 — the Overall-progress tiles are generated, and the generator prints the fix

`emit-dashboard` prints the `overall-progress` block ahead of the `stat-row` block, each pasteable alone. The region check is the same byte-compare contract as `-08`'s, through a shared helper with three distinct messages (absent marker, unclosed region, stale content), CRLF-normalised. `emit-dashboard` still does not run the dashboard check and still does not write the file — `-08` clause 1's reasons, preserved verbatim in the command's comment.

### Clause 2 — the surviving numerics are gated, not generated

Five extracted claims: tabstrip Epics count, tabstrip loose-end count, bar width, the footnote's four state counts, and `The Epic denominator is now N`. Each refusal message carries the expected text. No check writes or reads the prose around the numbers; the per-Story tables and UPDATE narrative are not parsed at all. The footnote's date is deliberately editorial: an emitter-stamped date would fail the byte-compare on every day boundary with no content change.

### Clause 3 — the Epic population is derived from disk

`roadmap_epics` (docs ∪ phase table, horizons excluded on both sides) and `decomposed_epics` (Story-id Epic tokens against the population) are pure functions with their own tests, including the case of a Story whose Epic is outside the population (`P9` when the fixture population lacks it) and an Epic with no Story.

### Clause 4 — every refusal is demonstrated

Fourteen host tests drive: a stale generated tile, an absent region, an unclosed region, a stale tabstrip Epic count, a stale tabstrip loose-end count, a hand-tuned bar width, stale footnote state counts, a stale Epic denominator — each beside its acceptance case (the emitted block passing its own check, a CRLF page passing, agreeing counts accepted, the rounding boundary cases including zero Stories). The committed tree additionally supplied the live refusal quoted above, which no green run could have.

### Clause 5 — corrected, not grandfathered

The tabstrip's "3 decomposed" was fixed in the same change that landed the gate. The four hand-written tiles were replaced by the generated region rather than left beside it.

## Security under load

Not applicable and not claimed. Nothing here runs at a measured performance point, admits code, crosses a containment boundary, or grants authority.

## Named debt this Report leaves open

- **The badge vocabulary remains a hand-maintained mapping** (`-08`'s named debt, untouched here).
- **The footnote gate assumes the four-state shape.** A fifth Story state reshapes the sentence by hand, with the gate refusing until it is — the intended direction of failure.
- **Epic-level story-count prose is still ungated** — e.g. the EPIC-P0 panel's "has since grown to 28" narrative predates `-07`/`-08`/`-09` and is now three Stories stale; it is argument, not a tile, and this Story deliberately does not own it.
- **`LE-34`** (`README.md`'s v1 list) **and `LE-65`** (headers versus their own Reports) **are untouched.**
- **No performance guardrail closes and no Story's assurance state moves.**
