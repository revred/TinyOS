# REPORT-2026-07-30-02 — The Spoor Gate Is Closed: Kernel-Audited Denials in the Parity Evidence Chain

**Test(s) covered:** `TEST-P2-07-01-A` (`STORY-P2-07-02`, `FEAT-P2-07`) — the parity gate,
grown its **third signal**. This is the dated record of executing
[`hand-2026-07-30/04A`](../../session/hand-2026-07-30/04A-spoor-gate-action-plan.md) (ordered
by the owner 2026-07-30 after `03A` recorded `LE-56`: *no console/tab run has ever captured a
kernel spoor*).
**Date:** 2026-07-30 · **Tier:** Tier 0 (QEMU x86_64) for the fixture and trailer; host-side
for the tab surfacing.
**Toolchain/commit:** pinned `nightly-2026-07-26` for the TinyOS tree; vendored Tauri fork
unchanged.
**Linked session:** `hand-2026-07-30/05A` ·
**Follows:** [`REPORT-2026-07-30-01`](REPORT-2026-07-30-01.md) (whose evidence review raised
`LE-56`).

## Non-claims — first

Denial spoors only: grant/outcome spoors are a named later seam. Host tabs remain
kernel-spoorless until the on-target tab host exists — the `SPOOR` verb renders their
injected view under the honest banner `host-side journal` (screenshot below shows exactly
that). No wire-format change to the spoor atom (`STORY-P0-06-01` not re-opened; taxonomy
additions only). No performance number: the `D14`/`D23` debt rows stand for
`STORY-P2-07-02` exactly as for `STORY-P2-07-01`.

## What was claimed and what held

| # | Claim (04A §2) | Verdict | Evidence |
|---|----------------|---------|----------|
| 1 | Fixture asserts `spoor_journal_len == denials == expected_denials()` in-guest (signal A) | **PASS** | Deliberate red first (`len == 0` asserted → `isa-debug-exit` failure observed under QEMU), then the real invariant → success exit. [`serial-capture-with-trailer.txt`](assets/2026-07-30-spoor-gate/serial-capture-with-trailer.txt) |
| 2 | Golden byte-comparison untouched; trailer parsed as signal C; success line names all three facts | **PASS** | `shell-parity: transcript matches golden (64 lines), the fixture's in-guest assertions passed, and the spoor journal corroborates the denial count (TINYOS-SPOOR/1 len=1 denials=1)`; splitter unit tests cover marker present/absent/malformed/mismatch — absent/malformed FAILs, never skips |
| 3 | Parity wall shows the spoor row; `smoke.json` carries `spoor_signal: true`; screenshots committed | **PASS** | [`06-parity-wall.png`](assets/2026-07-30-spoor-gate/06-parity-wall.png) — `three-signal rule — … spoor journal corroborates denials: green`, wall row `spoor journal corroborates denials (TINYOS-SPOOR/1)`, reserved region `parity suite: PASS`; [`03b-spoor-verb-host-side.png`](assets/2026-07-30-spoor-gate/03b-spoor-verb-host-side.png) — `SPOOR` at a live tab prompt; [`smoke.json`](assets/2026-07-30-spoor-gate/smoke.json) `"smoke": "pass"`, all 8 checks true |
| 4 | Decorator authorisation-neutral | **PASS** | `sp3_the_decorator_never_alters_the_wrapped_verdict` — verdict equality over the whole verb vocabulary, plus supervisor passthrough |
| 5 | Missing/malformed trailer fails closed | **PASS** | `shell_parity.rs` splitter tests (5 malformed shapes + absent + mid-line forgery) |
| 6 | Golden regenerated only via the `#[ignore]`d recorder, reviewed as a diff | **PASS** | The diff is exactly the three `SPOOR` output lines; committed in this change |
| 7 | Spine green; `LE-56` closed in the same commit as the last test | **PASS** | `check-assurance-spine` green with `STORY-P2-07-02` row, open-debt rows, `LE-56` closed |

## The transcript now shows the kernel audit trail

From the QEMU serial capture (the same bytes the golden encodes), after the second `DIR`:

```text
Spoor journal (policy-seam denial journal):
  #  CATEGORY    ACTOR     ACTION        OUTCOME     TARGET            COST
  1  shell       session   verb-denied   failed      clear-screen         0
Bad command or file name
DOS batch complete
TINYOS-SPOOR/1 len=1 denials=1
```

The one row is the withheld `CLS` denial, stamped as a kernel `Spoor`
(`Category::Shell`, `Actor::Session`, `Action::VerbDenied`, `Outcome::Failed`,
`TARGET` = the denied verb) at the `VerbPolicy::allows` decision point by the
journaling decorator, decoded back through `STORY-P0-06-01`'s own accessors for
rendering. The trailer after the transcript is the third signal's raw material; the
transcript before it byte-compares against the committed golden untouched.

## Where the pieces live

Kernel vocabulary: `os/src/kernel/src/spoor.rs` (`Category::Shell`, `Actor::Session`,
`Action::VerbDenied`; two `ACT` nibble values of headroom remain). Decorator + atomic
journal + render names: `os/src/shell/src/spoor_policy.rs` — beside the fixture, shared by
`#[path]` with the shell library's `#[cfg(test)]`-only include; the shipped `shell` library
stays `no_std`, kernel-free (`EPIC-P2` §3.2). Fixture: `fixture_batch_main.rs`. Splitter:
`os/src/xtask/src/shell_parity.rs`. Verb: `verbs.rs` (`VerbKind::SpoorJournal`, injected
`SpoorView`), register row `verb:spoor-journal` in `goals/context/terminal-gap.tsv`
(status `live-verified`). Tab: `stage-e-console` three-signal rule.
