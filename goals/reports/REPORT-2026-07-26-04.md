# REPORT-2026-07-26-04

**Test(s) covered:** [`TEST-P0-03-01-A`](../tests/TEST-P0-03-01-A.md) (pool alloc/free round-trip and invalid-handle rejection), [`TEST-P0-03-03-A`](../tests/TEST-P0-03-03-A.md) (pool exhaustion fails closed, then recovers after a free).

**Result:** **Pass.** `cargo test -p kernel --lib` runs 6 tests, all passing:

- `alloc_free_round_trip_returns_stored_value`, `freed_slot_is_reusable_by_a_later_alloc` — ordinary alloc/free round-trip (`TEST-P0-03-01-A`).
- `double_free_is_rejected_not_panicking`, `out_of_range_handle_is_rejected_not_panicking` — invalid-handle rejection (`TEST-P0-03-01-A`).
- `exhausted_pool_fails_closed_without_side_effects` — exhaustion fails closed and is transient, not latched (`TEST-P0-03-03-A`).
- `dropping_a_pool_with_occupied_slots_drops_their_values` — not part of either Test's written specification, added during implementation once `Drop` needed a correctness check of its own; kept because an untested `Drop` impl over `MaybeUninit` storage is exactly the kind of silent-leak defect this Feature exists to prevent.

Also verified:

- `cargo fmt -p kernel -- --check` — clean.
- `cargo clippy -p kernel --lib -- -D warnings` — clean, zero `#[allow]` exceptions needed.
- `cargo run -p xtask -- check-crate-sizes --ceiling=20000` — `kernel` at 308 lines (test module excluded from the count per the governance heuristic), trivially under ceiling.
- `cargo build -p kernel --target ../../targets/x86_64-tinyos.json -Z json-target-spec -Z build-std=core,compiler_builtins -Z build-std-features=compiler-builtins-mem` — the `no_std`/`no_main` kernel binary still builds cleanly against the real target after adding the `[lib]` target alongside the existing `[[bin]]`, confirming the `#![cfg_attr(not(test), no_std)]` split doesn't regress the walking skeleton from `FEAT-P0-01`.

**Environment/tier:** Host unit test (no QEMU/hardware dependency) — `os/src/kernel/src/mem.rs`'s allocator logic depends only on `core` semantics common to host and `no_std` builds, per `TEST-P0-03-01-A`/`TEST-P0-03-03-A`'s Tier classification. QEMU was not available in this environment, so `STORY-P0-02-*`/`STORY-P0-04-*` (which need Tier 0 verification) were decomposed into Stories this session but not implemented — see the linked session handover.

**Toolchain/commit:** `nightly-2026-07-26` (workspace-pinned toolchain, per `os/rust-toolchain.toml`); `cargo 1.96.0`, `rustc 1.96.0`.

**Linked session:** [`session/hand-2026-07-26/09-feat-p0-03-decomposition-and-pool-allocator.md`](../../session/hand-2026-07-26/09-feat-p0-03-decomposition-and-pool-allocator.md).

**Implementation note:** `Pool<T, N>::alloc`/`free` take `&mut self`, not `&self` — deliberate per `STORY-P0-03-01`'s acceptance criteria: no concurrent-access story exists yet (no scheduler, `FEAT-P0-02`, is implemented), so this Story doesn't invent a locking/interior-mutability scheme speculatively. `Pool` also implements `Drop` (dropping any still-occupied slots) even though no Story explicitly asked for it — an allocator that leaks its contents on scope-exit would itself be a defect, not a missing nice-to-have, so it's treated as implied by `STORY-P0-03-01`'s acceptance criteria rather than deferred to a new Story.
