# REPORT-2026-07-26-15

**Test(s) covered:** `TEST-P0-06-01-A` (`STORY-P0-06-01` — core packed `Spoor` type).

**Result:** **Pass.**

- `cargo test -p kernel --lib` — 7 new `spoor` tests, all passing:
  - `every_field_combination_round_trips_through_stamp_and_accessors` — exhaustive over every `Category` (7) × `Actor` (2) × `Action` (7) × `Outcome` (8) combination (784), each crossed with 4 representative `target`/`cost` boundary/mid-range samples (16 pairs), 12,544 total combinations — every accessor returns exactly the value it was stamped with, no lossy bit overlap between fields.
  - `spoor_is_exactly_eight_bytes` — `size_of::<Spoor>() == 8`.
  - `decode_round_trips_a_validly_stamped_spoors_bits` — `decode(stamp(...).to_bits())` returns the identical `Spoor`.
  - `decode_rejects_an_unknown_category_nibble`, `decode_rejects_an_unknown_actor_nibble`, `decode_rejects_an_unknown_action_nibble`, `decode_rejects_an_unknown_outcome_nibble` — each field's out-of-range nibble value fails closed with its own distinct `SpoorError` variant, verified individually (the other three fields' bits held at a valid value so only the field under test is exercised).
- `cargo test --workspace --lib` — 104/104 passing (up from 97): `hal` 4, `hal-x86_64` 26, `kernel` 43 (36 + 7 new in `spoor`), `exec` 31.
- Regression check on all five pre-existing QEMU fixtures, same session: `cargo run -p xtask -- qemu-x86_64` (no `--fixture`), `--fixture=broken-boot` (exits `KernelBootFailed` by design), `--fixture=context-switch`, `--fixture=address-space`, `--fixture=win32-shim` — all still pass unchanged. This Story added no QEMU fixture: `kernel::spoor` is pure bit-packing logic with no target-specific dependency, mirroring `STORY-P0-02-01`'s own host-only precedent.
- `cargo fmt --check` (workspace) — clean.
- `cargo clippy --workspace --lib --all-targets -- -D warnings`, restricted to host-buildable targets (`kernel`'s and `exec`'s `[[bin]]` targets still can't be host-compiled on this Windows dev machine — same pre-existing, already-documented limitation, not a regression) — clean.
- `cargo run -p xtask -- check-crate-sizes --ceiling=20000` — `kernel` 1437 lines (up from 1115 — new `spoor.rs`), `exec` 1198, `hal-x86_64` 750, `hal` 91, `xtask` 535 — all comfortably under the ceiling.

**Environment/tier:** Host only (`cargo test -p kernel --lib`), per `TEST-P0-06-01-A`'s own scope — bit-packing logic has no target-specific dependency.

**Toolchain/commit:** `nightly-2026-07-26` (workspace-pinned toolchain, per `os/rust-toolchain.toml`); `cargo 1.96.0`, `rustc 1.96.0`.

**Linked session:** [`session/hand-2026-07-26/20-strategic-objectives-spoor-ipc-blue-sharc-correction.md`](../../session/hand-2026-07-26/20-strategic-objectives-spoor-ipc-blue-sharc-correction.md).

**Deliberately not done:**

- **No journal writer.** `STORY-P0-06-02` (append-only spoor journal) is a separate, not-yet-implemented Story — this Report covers only the `Spoor` value type itself.
- **No subsystem actually emits spoors yet.** `kernel::lock`, `kernel::wcet`, `kernel::dispatch` don't call `Spoor::stamp` anywhere — `FEAT-P0-06`'s own exit criteria explicitly track this as a follow-up Story once `-01`/`-02` land, not a gap in this specific Story's own acceptance criteria.
- **`Category`'s and `Action`'s vocabularies are TinyOS's own, not `Sharc.Blue`'s.** Only the bit layout and `Outcome`'s vocabulary are adopted verbatim — documented explicitly in `kernel::spoor`'s own doc comment and `STORY-P0-06-01.md`'s scope note, since `CAT`/`ACT` are inherently per-project taxonomies within a shared wire format, not something to copy from an unrelated domain.
- **`Category`/`Action` each leave 9 of 16 possible nibble values unassigned**, and `Actor` leaves 14 of 16 unassigned — deliberately not pre-filled with speculative "Reserved" placeholders; a new subsystem that starts emitting spoors gets a new variant added when it exists, per the module's own doc comment.
