# REPORT-2026-07-26-25

**Test(s) covered:** [`TEST-P0-01-02-A`](../tests/TEST-P0-01-02-A.md) (five-class containment-contract and assurance-spine governance extension)

**Result:** **Pass (structural governance integrity only; runtime containment debt remains).**

- `cargo test -p xtask`: 18/18 pass, including complete/missing containment-class fixtures, missing boundary-test fixtures, security/performance catalogue fixtures, and the committed-spine test.
- `cargo run -p xtask -- check-performance-catalogue`: 625/625 unique `D01..D25 × G01..G25` cells validate.
- `cargo run -p xtask -- check-assurance-spine`: 8 Features, 25 Stories, 22 Test documents, 25 Reports, 5 containment classes, 20 boundary tests, 20 security controls, and 1,025 selected Story/performance contracts validate.
- The gate rejects a missing/malformed `C0..C4` class, missing/malformed `BND-01..BND-20` boundary test, incomplete Feature contract coverage, an unknown or unowned boundary test, a Story class outside its Feature contract, and a security control whose classes do not intersect the Story.
- Every existing Phase-0 Feature has an explicit implementation class, subject classes, authority posture, hostile-input statement, boundary-test selection, and evidence requirement.
- Every existing Story and all 20 security controls name their applicable containment classes.
- Every existing Test document carries exact performance-domain, security-control, containment-class, boundary-test, and assurance-state metadata; the checker compares it with the Story and parent Feature and rejects drift.

**Environment/tier:** Host/CI governance meta-test on Windows; no hardware tier.

**Toolchain/commit:** Workspace-pinned `nightly-2026-07-26`; dirty working tree, so no commit hash is claimed.

**Assurance IDs:** `PERF-D25-G01` through `PERF-D25-G25`; `SEC-03`, `SEC-05`, `SEC-09`, `SEC-13`, `SEC-14`, `SEC-16`, `SEC-18`, `SEC-19`, `SEC-20`; `C0` through `C4`; `BND-01` through `BND-20`.

**Raw evidence:** The raw evidence is the checker output and passing negative-fixture suite. It proves catalogue completeness, referential integrity, Feature/Story ownership, and class compatibility. It does not prove active page-table isolation, sandboxing, verified boot, driver/IOMMU isolation, quarantine, promotion, teardown, or campaign resistance.

**Security under load:** Deferred and release-blocking. No hostile runtime load, cross-address-space probe, DMA attack, promotion race, parser exploit, timing sample, or hardware test was run by this governance meta-test. `STORY-P0-01-02` therefore remains assurance `baseline-debt`.

**Linked session:** [`session/hand-2026-07-26/30-five-class-containment-contracts.md`](../../session/hand-2026-07-26/30-five-class-containment-contracts.md).
