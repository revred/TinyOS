# REPORT-2026-07-26-14

**Test(s) covered:** `TEST-P0-03-02-A` (`STORY-P0-03-02` — compile-time pool-size configuration).

**Result:** **Pass.**

- `cargo test -p kernel --lib` — 1 new test: `capacities::tests::committed_capacities_fit_within_the_documented_budget` (a runtime-visible restatement of the `const _: () = assert!(...)` check every successful build of this crate already proves).
- `cargo run -p xtask -- governance-fixture-test` — extended from four fixtures to five: the new `fixture-capacity-budget` writes a self-contained crate with a deliberately-violated `const`-evaluated capacity/budget check (mirroring `kernel::capacities`'s own pattern, not referencing that crate directly — the property under test is "this *style* of check fails the build," not any specific crate's current values) and confirms `cargo build` and `cargo clippy` both fail with `error[E0080]` (a `const`-evaluation panic), never succeeding as if the violation were silently accepted. `cargo fmt --check` still passes on the fixture (it's well-formatted; formatting doesn't require successful compilation). `cargo doc`'s pass/fail was deliberately not asserted on — empirically, rustdoc does not force the same const-evaluation `cargo build`/`clippy` do, so a crate that fails to build can still successfully document; asserting on that would be asserting an accident of rustdoc's own implementation, not anything this Story's acceptance criteria care about.
- `cargo test --workspace --lib` — 97/97 passing (up from 96): `hal` 4, `hal-x86_64` 26, `kernel` 36 (35 + 1 new in `capacities`), `exec` 31.
- Regression check on all five pre-existing QEMU fixtures, same session: `cargo run -p xtask -- qemu-x86_64` (no `--fixture`, exercises `MAX_CPUS` via the real boot-time ACPI topology discovery path — confirms the `kernel::capacities` import didn't break the real consumer), `--fixture=broken-boot` (exits `KernelBootFailed` by design), `--fixture=context-switch`, `--fixture=address-space`, `--fixture=win32-shim` (the latter two now import `EXEC_FRAME_POOL_CAPACITY` from `kernel::capacities` instead of a local `FRAMES` constant) — all still pass unchanged.
- `cargo fmt --check` (workspace) — clean.
- `cargo clippy --workspace --lib --all-targets -- -D warnings`, restricted to host-buildable targets (`kernel`'s and `exec`'s `[[bin]]` targets still can't be host-compiled on this Windows dev machine — same pre-existing, already-documented limitation, not a regression) — clean. Substitute check for the two `exec` `[[bin]]` targets against the real target (both now import `kernel::capacities::EXEC_FRAME_POOL_CAPACITY`): `cargo clippy -p exec --bin exec-fixture --bin win32-shim-fixture --target targets/x86_64-tinyos.json -Z json-target-spec -Z build-std=core,compiler_builtins -Z build-std-features=compiler-builtins-mem -- -D warnings` — clean.
- `cargo run -p xtask -- check-crate-sizes --ceiling=20000` — `kernel` 1115 lines (up from 1057 — new `capacities.rs`), `xtask` 535 lines (up from 481 — the new `check_capacity_budget_fixture`), `exec` 1198 (unchanged — the fixture files' net line count didn't grow, a `const FRAMES` line traded for a `use` line), `hal-x86_64` 750, `hal` 91 — all comfortably under the ceiling.

**Environment/tier:** Host only (`cargo test -p kernel --lib`, `cargo run -p xtask -- governance-fixture-test`), per `TEST-P0-03-02-A`'s own scope — `kernel::capacities` is pure `const` configuration with no target-specific dependency, and the build-time property under test (a violated budget fails compilation) is provable identically on the host toolchain. The real-target rebuild of `exec`'s two fixtures (now importing from `kernel::capacities`) and the unchanged QEMU boot behavior additionally confirm the consolidation didn't disturb either real consumer.

**Toolchain/commit:** `nightly-2026-07-26` (workspace-pinned toolchain, per `os/rust-toolchain.toml`); `cargo 1.96.0`, `rustc 1.96.0`.

**Linked session:** [`session/hand-2026-07-26/19-story-p0-03-02-capacity-configuration-implementation.md`](../../session/hand-2026-07-26/19-story-p0-03-02-capacity-configuration-implementation.md).

**Deliberately not done:**

- **No task-control-block, IPC-message, or demand-paging pool capacity declared.** Per this Story's own scope-resolution note: none of the three has a real production call site yet (no `Scheduler` is wired into `main.rs`'s boot path; no IPC or demand-paging subsystem exists at all). Adding a placeholder constant with nothing to consume it would be speculative, ahead-of-need code this codebase's own standards argue against — add one alongside whichever future Story first wires a production consumer in.
- **`committed_bytes()`'s accounting is an approximation, not exact `Topology<N>`/`Pool<T, N>` byte layout.** It multiplies each capacity by its element type's own `size_of`, ignoring `Option`/`MaybeUninit` tag/padding overhead (both crates' implementation details `kernel::capacities` has no visibility into from outside). Adequate headroom for a budget *ceiling* check at Phase 0's capacity sizes — documented as approximate in the module's own doc comment, not claimed byte-exact.
- **`STATIC_MEMORY_BUDGET_BYTES` (8 MiB) is not a hardware-measured figure.** Chosen to match `G-DX-8`'s existing 8MB total-image ceiling as the nearest existing reference point, since no real Tier 2 target hardware has been purchased yet to measure an actual RAM budget against — flagged in the module's own doc comment as worth revisiting once it is (`EPIC-P0`'s Tier 2 HIL step).
