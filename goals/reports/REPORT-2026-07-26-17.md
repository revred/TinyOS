# REPORT-2026-07-26-17

**Test(s) covered:** `TEST-P0-06-03-A` (`STORY-P0-06-03` — wire `kernel::lock` to emit spoors on boost/restore, closing `FEAT-P0-06`'s own exit-criteria follow-up).

**Result:** **Pass.**

- `cargo test -p kernel --lib` — 49/49 passing (unchanged count from `REPORT-2026-07-26-16`: no new test functions were added — `kernel::lock`'s existing `STORY-P0-02-03` tests were extended in place with spoor-emission assertions, and `kernel::dispatch`'s existing dispatch-round test was updated to thread a journal through). All seven `lock::tests` pass with the new `&mut SpoorJournal<J>` parameter; `dispatch::tests::dispatcher_runs_the_boosted_holder_ahead_of_an_uninvolved_ready_task_after_contention` passes unchanged.
- `cargo test --workspace --lib` — 110/110 passing (unchanged from `REPORT-2026-07-26-16`): `hal` 4, `hal-x86_64` 26, `kernel` 49, `exec` 31.
- Regression check on all five pre-existing QEMU fixtures: `cargo run -p xtask -- qemu-x86_64` (no `--fixture`), `--fixture=broken-boot` (exits `KernelBootFailed` by design), `--fixture=context-switch`, `--fixture=address-space`, `--fixture=win32-shim` — all still pass unchanged. This Story added no QEMU fixture and no production call site (no dispatcher is wired into `main.rs` yet, per `kernel::capacities`' own precedent) — the release kernel ELF is unchanged at 16,032 bytes, confirmed by a clean rebuild.
- `cargo fmt --check` (workspace) — clean.
- `cargo clippy --workspace --lib -- -D warnings`, restricted to host-buildable lib targets (`kernel`'s and `exec`'s `[[bin]]` targets still can't be host-compiled on this Windows dev machine — same pre-existing, already-documented limitation, not a regression) — clean.
- `cargo run -p xtask -- check-crate-sizes --ceiling=20000` — `kernel` 1583 lines (up from 1539 — `lock.rs`'s new journal parameter and stamped-event doc comments, `dispatch.rs`'s updated test), `exec` 1198, `hal-x86_64` 750, `hal` 91, `xtask` 659 — all comfortably under the ceiling.

**Environment/tier:** Host only (`cargo test -p kernel --lib`), per `TEST-P0-06-03-A`'s own scope — `kernel::lock`/`kernel::dispatch`/`kernel::spoor_journal` are pure logic with no target-specific dependency.

**Toolchain/commit:** `nightly-2026-07-26` (workspace-pinned toolchain, per `os/rust-toolchain.toml`); `cargo 1.96.0`, `rustc 1.96.0`.

**Linked session:** [`session/hand-2026-07-26/23-story-p0-06-03-spoor-adoption-kernel-lock.md`](../../session/hand-2026-07-26/23-story-p0-06-03-spoor-adoption-kernel-lock.md).

**Deliberately not done:**

- **No production call site.** No dispatcher is wired into `main.rs` yet — `kernel::capacities`' own stated convention ("only capacities with a real, concrete production or Tier-0-fixture consumer are declared here today") means no `SpoorJournal<N>` capacity constant or D11-G09/G10 footprint measurement is added yet either; that's the next natural step once a real caller picks a concrete `N`, not before.
- **`kernel::wcet`'s overrun detection left unwired.** `FEAT-P0-06`'s exit criteria only required *at least one* real subsystem; `kernel::wcet::record_tick`'s `Action::Overrun`/`Action::ResetBudget` remain available and unused, a natural next candidate if a second subsystem is wanted.
- **No storage-backed journal, no query/replay/summarize API** — unchanged from `REPORT-2026-07-26-16`'s own scope notes.
