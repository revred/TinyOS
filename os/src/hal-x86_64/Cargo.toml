[package]
name = "hal-x86_64"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "x86_64-specific HAL backend. Phase 0: empty shell."

[dependencies]
hal = { path = "../hal" }
