[package]
name = "motion"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Motion-group synchronisation contracts: typed identities, fixed 16/32 frames, whole-epoch validation, the MotionGroupTransport contract and its deterministic in-memory double (STORY-P1-08-01, ADR 0010)."

[dependencies]
