[package]
name = "hal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Arch-neutral HAL: bus enumeration, ACPI/Device-Tree manifest normalization, HAL trait definitions. Phase 0: empty shell."

[dependencies]
