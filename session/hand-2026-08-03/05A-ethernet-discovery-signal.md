# Handover 05A (2026-08-03) — 04A Executed: the Card Gains Network Smarts, Honestly

Session order, verbatim in effect: *"load as context and execute to deliver
[`04A`](04A-tinyremote-vision.md)"*, then — answering this session's honest
objection that nothing network-related could go onto the card while `LE-26`'s
chain queued behind serial bring-up — *"The objective of this session is to fix
it."* That is the owner reorder 04A reserved for itself, and this session
executed it. Also ordered mid-session: **TinyRemote is renamed Ti64Dink** (04A's
text stays as written; dated records are immutable — this document is the
rename's record of note).

## What was delivered

**[`FEAT-P1-09`](../../goals/features/FEAT-P1-09.md) — Ethernet as a Discovery
Signal: RP1 over the Firmware-Kept PCIe Link** — promoted through the full
spine (Feature + three Stories + three TESTs + contract rows + `D20` open-debt
row + `LE-67`) and host-implemented the same day. The scope trick that made a
driver Epic into a bring-up Feature: the Pi 5 firmware raises the PCIe ×4 link
to RP1 itself and only *resets* it before kernel entry — `config.txt` gains
`pciex4_reset=0` (now **generated by the build** and pinned, no longer
folklore), and the kernel interrogates the always-mapped PCIe2 controller
registers (status gates, then outbound-window readback) before the one read
through the `0x1F_0000_0000` window that identifies the GEM. No data-abort
recovery machinery was needed and none was built; a reset link reports
`rp1=absent reason=port-not-rc` from registers that always answer.

The three Stories, all host halves Green (`cargo test -p hal-arm64`: 171,
`-p xtask`: 266; cross-target clippy `-D warnings` clean; spine green):

1. **[`STORY-P1-09-01`](../../goals/stories/STORY-P1-09-01.md)** — the kept
   link. New `pcie.rs` (gates + window decode, every rejection driven) and
   `board.rs` constants transcribed from `rpi-6.12.y` (`bcm2712.dtsi`,
   `pcie-brcmstb.c`, `rp1.dtsi`) with the same source-revision discipline and
   32-bit-truncation trap tests as the UART's.
2. **[`STORY-P1-09-02`](../../goals/stories/STORY-P1-09-02.md)** — the PHY
   answers. New `gem.rs`: clause-22 framing pinned to the bit, bounded idle
   polls, identity-before-belief scan (expected BCM54213PE, revision steppings
   tolerated, unknown parts reported and believed for nothing), latched-twice
   link state with downward speed resolution. This is the device half of "the
   cable is the signal" — the board can now *see* the cable.
3. **[`STORY-P1-09-03`](../../goals/stories/STORY-P1-09-03.md)** — the beacon.
   One raw Ethernet II broadcast (`FF…FF` ← `02:54:4F:53:36:34`, EtherType
   `0x88B5`) carrying `TOS64-PRESENT/1 board=pi5-bcm2712 seq=N`, built pure and
   pinned; a two-descriptor 64-bit ring whose second descriptor is a permanent
   stop; transmit order and bounds asserted by a recording double; **receive
   never enabled, and that absence tested**. After the verdict and splash the
   board emits one `TOS64-LINK/1` line and then beacons once per second —
   which makes a stock packet capture on the laptop the Feature's evidence
   channel *before the serial adapter works*.

New wire envelopes, both per the `TOS64-*` single-line convention:
`TOS64-LINK/1` (serial, one line, honest in both directions) and
`TOS64-PRESENT/1` (the beacon payload).

## The card

Re-staged and hash-verified on `E:` (TOS64BOOT): `kernel8.img` 108,460 bytes,
sha256 `e77aeb88…dae217c`, plus the generated three-line `config.txt`. Earlier
in the session the card was found already carrying the pre-network image
byte-identical to a fresh HEAD build (`18a28448…`) — verified in place, not
rewritten; the network image above superseded it.

## What is *not* claimed

- **Nothing has run on silicon.** Every board criterion (both `config`
  variants, cable in/out, the beacon in a capture) is open, blocked on the
  same serial adapter as `FEAT-P1-07` (`LE-66`'s origin — three silent
  captures). The beacon partially routes around that blocker as evidence, but
  bring-up debugging still needs the UART.
- **Named blind-flight risks**, recorded in the Stories rather than papered
  over: PHY power/reset provisioning and the RGMII transmit clock are believed
  firmware/hardware-default; the brcmstb window encoding and GEM bit positions
  are transcriptions (`board.rs`/`pcie.rs`/`gem.rs` doc comments carry the
  source revisions). A wrong transcription fails as an honest `rp1=absent` or
  a silent capture at `link=up` — both diagnosable outcomes, both named.
- **`LE-26` stays open.** A firmware-kept window is not a PCIe driver; the RP1
  C2 device service, NIC class driver, IRQ path and the transport re-opening
  are still owed (`EPIC-P3`/`EPIC-P1_5`). **`LE-67`** (new) names the
  IOMMU-less transmit DMA: the containment story is one pinned 64-byte-aligned
  static buffer and receive disabled, nothing stronger.
- **No timing claim**; `D20` is stated open debt on `STORY-P1-09-03`.

## Ti64Dink (né TinyRemote)

The host application is renamed **Ti64Dink** by owner order. Its promotion is
*not* done this session: per the console-side reconnaissance, it belongs in
`external/tauri/tinyos-poc` beside the Ti64 console (ADR 0008's one-way
isolation), under `EPIC-P2` as a **new** Feature — `FEAT-P2-10` is the first
free slot — with the TinySpot/`APP-15` settlement made at that promotion, not
before. The console's `commands.rs` seams and serde DTOs are already
wire-shaped; `send_line`'s `ONE_WAY_TRANSPORT` refusal is the honest hole a
future bidirectional link closes.

## Next session, in order

1. **Loopback-test the serial adapter** (unchanged; still the queue head).
2. Board run of the new image: capture both `config.txt` variants
   (`TEST-P1-09-01-A` clause 6), cable in/out (`-02-A` clause 6), and a
   laptop-side packet capture of the beacon (`-03-A` clause 6) — Wireshark or
   `tcpdump`, filter `ether proto 0x88B5`. File the Report with the capture as
   raw evidence.
3. If the beacon appears: that is 04A's "earliest visible win" delivered on
   silicon, and the Ti64Dink link-watch (host side, `FEAT-P2-10`) becomes the
   natural next promotion.

Session hygiene: no concurrent commits landed mid-session; `work/tools/sdprep`
stays deliberately untracked (02A's pending-ADR state); the running soak's log
(`goals/reports/_soak-p0-03-01.log`) was left out of the commit.

## Board attempt, same evening — negative evidence, recorded

The owner staged the card, cabled the Pi 5 peer-to-peer to the laptop's
Realtek GbE port, and powered it. Observed, with the host as instrument:

- **Power LED green, steady** — firmware ran, no error blink pattern.
- **Ethernet PHY dead flat**: the laptop NIC stayed `Disconnected / 0 bps`
  through ~2.5 minutes of watching and across **three power cycles** (one at
  4 Hz polling) — not even a transient training blip. This is the first real
  evidence against `STORY-P1-09-02`'s recorded belief that the PHY comes up
  on hardware defaults: the BCM54213PE is most likely unpowered or held in
  reset until software releases it (as Linux's driver does). Registered as
  `LE-68`.
- **Fourth silent serial capture** (`runs/1785764707-boot`, 0 bytes, exit 3)
  on the adapter now enumerated as COM5 (USB VID 3679). The loopback test
  (`TEST-P1-07-01-A` clause 1) was set up but **not executed** — the session
  ended before the TX–RX bridge was made. The adapter remains unverified and
  the triage order unchanged: loopback first, connector muxing second.
- **No HDMI signal** across a power cycle with a monitor pre-connected —
  *conditions unconfirmed* (which micro-HDMI port and monitor-input state
  were not established), so this neither confirms nor refutes the splash;
  `STORY-P1-07-07`'s board criterion stays exactly as open as it was. Worth
  noting: no firmware diagnostic screen appeared either, which on a Pi 5 is
  more consistent with a successful handoff into the image than with a boot
  failure.

Net: the board very likely boots and parks green with all three output
channels dark for three *independent* reasons — splash (unproven mailbox
path), Ethernet (`LE-68`), serial (unverified wiring). Owner called it:
back to the drawing board. The one test that splits the space, still undone,
is the sixty-second adapter loopback.

## The constraint round: `STORY-P1-09-05`, the heartbeat on every channel

The owner then drew the real boundary — no monitor experiments, no Ethernet,
no bridging wires, *"solve with the constraints we have"* — and asked the
question this Story answers: **"what stopped you from making a ball bounce?"**
Acknowledged in the Story itself: nothing but a too-literal reading of 03A's
no-display-investment line. The board's problem all along was that it speaks
*once*, at boot, so hearing it required wiring ∧ clock ∧ operator timing to
hold simultaneously. `STORY-P1-09-05` (host Green same evening; 182 hal-arm64
tests) removes the timing term and adds a second channel:

- **Serial:** the park loop emits `TOS64-BEAT/1 seq=N state=…` every second,
  forever — so the host can listen at leisure and *sweep bauds*; a wrong UART
  clock becomes bytes at a findable rate instead of silence, and the first
  contact of any kind also carries the `TOS64-LINK/1` verdict.
- **Screen:** the splash surface, if the firmware granted one, animates — a
  verdict-colored block (green = beaconing, amber = parked) bouncing at
  ~10 Hz. A monitor connected from power-on shows *motion* whenever the board
  is alive; a dark screen under exactly those conditions finally becomes
  evidence against the mailbox path itself.

Each channel stops independently and fail-safe (wedged UART, stuck counter);
the pinned protocol lines and the single LINK line are untouched. Image:
**117,780 bytes, sha256 `114bc9b3…418374c2`** — the card was in the Pi at
build time and **must be re-staged with this image** for the untimed
experiment to begin.

## Same evening, continued — `LE-68`'s answer found and implemented

The owner said "lets go", the two commits were pushed (**CI green**, run
`30821909147` watched to completion per the `LE-64` rule), and the drawing
board gave up the answer in under an hour: `bcm2712-rpi-5-b.dts` carries
`phy-reset-gpios = <&rp1_gpio 32 GPIO_ACTIVE_LOW>` with
`phy-reset-duration = <5>` — the PHY sits behind an **active-low reset on RP1
GPIO 32** that only an OS driver ever releases. The dead-flat wire was never
a mystery; it was a pin.

Delivered as **[`STORY-P1-09-04`](../../goals/stories/STORY-P1-09-04.md)**
(host half Green same evening; 176 hal-arm64 tests): new `rp1_gpio.rs` with
the bank-1 transcriptions (`io_bank1`/`sys_rio1`/`pads_bank1` at one `0x4000`
stride, GPIO 32 = pin 4, RIO funcsel 5, atomic set/clear aliases, pad
output-disable bit 7) pinned the `board.rs` way, and a glitch-ordered release
— pad enable → level low + direction out through RIO → funcsel hands RIO the
pin (first driven value *is* the assertion) → 5 ms hold → high → settle —
run exactly once between GEM identity and the MDIO scan. A stuck counter
aborts with the line still asserted and reports `phy=unreleased`; a pipeline
that never reaches identity never touches the GPIO. `LE-68` stays open until
the board's link watch shows a training transition — that is the criterion.

Image rebuilt: **109,276 bytes, sha256 `0f8cf11b…a23550ac`**. The card was
back in the Pi by then and was re-staged with `0f8cf11b…` (hash-verified on
`E:`) once the owner remounted it.

### Second board attempt, release-enabled image — still flat

Card carrying `0f8cf11b…` (PHY release included), cable connected, powered:
the laptop NIC stayed `Disconnected / 0 bps` through a four-minute watch at
4 Hz spanning the power-on. Not a transient. The result does **not** localise
the fault — at least four stories fit and they produce *different*
`TOS64-LINK/1` lines nobody can read yet: (a) the firmware ignored or
overrode `pciex4_reset=0` and the probe honestly reported `rp1=absent`
without ever touching the PHY; (b) the probe passed but a bank/alias/pad
transcription in the release is wrong; (c) the release ran and the PHY needs
more than reset (a power rail, a clock); (d) the discovery path faulted
after the splash and the board parked on the fault report. **Every branch is
one serial capture away from being distinguished, and zero branches are
distinguishable without it.** The adapter loopback (offered twice, TX–RX
bridge at the adapter end, sixty seconds) is now the sole gate on all
further Ethernet diagnosis; it was still unexecuted when the session record
closed.
