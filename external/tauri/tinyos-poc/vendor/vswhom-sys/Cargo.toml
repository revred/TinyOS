[package]
name = "vswhom-sys"
version = "0.1.3"
edition = "2018"
publish = false
description = "Pure-Rust stub of vswhom-sys for machines with no Visual Studio: always reports 'not found'. Build-tooling only; see README-TINYOS.md."

[dependencies]
libc = "0.2"
